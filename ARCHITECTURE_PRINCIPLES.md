# 仓库设计原则：边界、优雅、性能

本仓库的所有改动都应同时满足三个关键词：**边界、优雅、性能**。

这不是只针对某个 commit 或某条链路的要求，而是用于整个仓库的长期工程判断标准。无论是同步上游、修复 bug、增加兼容层、调整 gateway、改后台任务，还是优化管理接口，都应先用这三个词检查设计是否成立。

## 总纲

**边界清楚，逻辑才可靠；实现优雅，系统才可维护；性能克制，服务才扛得住。**

更工程化地说：

**所有改动都必须限定作用域、解决根因、保持热路径克制。**

如果一个改动说不清：

- 它属于哪条链路、哪个层级、哪个责任域；
- 它解决的根因是什么，为什么放在这个位置；
- 它是否让无关请求、无关模型、无关模块承担额外成本；

那么这个改动就不应该直接进入仓库。

## 1. 边界

边界是第一原则。任何改动都必须先回答：**这段逻辑属于哪条链路、哪个层级、哪个责任域？**

### 链路边界

不同入口、不同上游、不同协议必须分开处理，不能因为表面相似就共享一套隐式逻辑。

例如，以下链路应保持清晰隔离：

- OpenAI APIKey 原生链路；
- OpenAI OAuth / Codex 链路；
- Anthropic Messages compatibility 链路；
- Responses API 原生链路；
- Chat Completions fallback 链路；
- 国产 OpenAI-compatible 模型链路，例如 DeepSeek、Kimi、Moonshot、GLM、Qwen、Minimax。

某个链路的兼容逻辑不能污染另一条链路。不能因为都长得像 OpenAI API，就把所有行为塞进 OpenAI gateway 通用路径。

### 协议边界

客户端传什么、服务端补什么、上游要求什么，必须区分。

服务端可以做兼容，但兼容行为必须有明确边界：

- 明确适用的模型、供应商、账户类型或入口路径；
- 明确为什么需要服务端介入；
- 明确不启用时不会影响其他路径；
- 明确不会改变客户端原始意图。

不要无边界替客户端猜协议参数。fallback、自动填充、请求改写都必须 scoped。

### 职责边界

每层代码只做自己负责的事情：

- adapter 负责协议适配；
- request construction 负责构造正确请求；
- retry 负责错误后的恢复，不负责掩盖错误请求构造；
- billing 负责计费归因；
- scheduler 负责调度；
- repository 负责数据访问；
- policy / capability detection 负责能力和策略判断。

一个 helper 不应该横跨多个语义领域，变成“什么都管”的隐式魔法。

### commit / PR 边界

一个 commit 或 PR 应解决一个明确问题，并且问题边界可解释、可测试、可回滚。

不接受把 gateway、pricing、scheduler、OAuth、国产模型、ReasoningEffort、retry、middleware 等不相干内容揉成一个“大修复”。

同步上游时也必须 selective：先审问题、审必要性、审是否符合本地架构，再决定是否移植。上游有某个实现，不等于本地应该原样照搬。

### 判断标准

问自己：

- 这个逻辑是否只影响它应该影响的链路？
- 某个供应商或模型的特殊性是否泄漏到了通用路径？
- 这个 helper 的职责能否用一句话说清？
- 这个 commit 是否能单独解释、单独测试、单独回滚？

一句话：**边界就是不让一个场景的特殊性泄漏到整个系统。**

## 2. 优雅

优雅不是少写代码，而是**改在正确位置，用最小但完整的设计解决真实问题**。

### 修根因，不修症状

错误发生在哪里，就从哪里找根因：

- 请求构造错，就修请求构造；
- 状态管理错，就修状态管理；
- 协议映射错，就修协议映射；
- 数据查询错，就修查询条件；
- 缓存失效错，就修失效路径。

retry、兜底、特殊 if、错误后清理都不能替代根因修复。它们可以作为防御层，但不能用来掩盖本来可以构造正确、映射正确、状态正确的问题。

### 结构和问题一致

代码结构应反映问题本身的结构。

如果问题只属于某个 adapter，就不要改 gateway 通用路径；如果问题只属于某类模型，就不要影响全部 OpenAI-compatible 请求；如果问题只属于 OAuth/Codex，就不要让 APIKey 路径承担它的规则。

### 简洁但不粗糙

好的实现应该容易解释：

- 命名表达真实语义；
- helper 小而明确；
- 特殊规则集中在合适的 adapter / policy 层；
- 注释解释非显然的保护逻辑，而不是替混乱结构补文档；
- 测试证明真实边界和真实行为。

避免堆 if/else、模型名前缀散落、跨层读取状态、把多个不相干问题绑在一起。

### 兼容逻辑可读、可证、可删

兼容层必须回答四个问题：

- 为什么兼容；
- 兼容谁；
- 什么时候启用；
- 将来不需要时能否干净删除。

如果一段兼容逻辑无法说明这些问题，它就很可能是 ad-hoc patch，而不是可维护设计。

### 测试验证设计边界

测试不只是覆盖代码行，而是证明设计边界没有被打破。

例如：

- APIKey 不该受 OAuth 行为影响；
- OpenAI 原生链路不该受国产模型行为影响；
- retry 成功不代表首次请求构造正确；
- fallback 不应在未命中目标模型/供应商时改变请求；
- 管理端过滤不应改变无关列表语义。

一句话：**优雅就是让代码的结构和问题本身的结构一致。**

## 3. 性能

性能不是最后优化，而是设计约束。这个仓库包含 gateway 热路径、流式转发、计费、调度、缓存、管理查询等高频或高成本逻辑，不能把“能跑”当成合格。

### 热路径少做事

gateway 和请求转发路径应尽量避免不必要工作：

- 不必要不全量 Unmarshal；
- 能轻量提取就轻量提取；
- 能懒解析就懒解析；
- 不为某个特殊模型需求让所有请求都付出解析、转换、复制成本；
- 不在流式路径上引入非必要全量缓冲。

### 避免无意义重试和二次请求

retry 是恢复机制，不是常规控制流。

如果首次请求能构造正确，就不要靠失败后修剪再发一次。不要用额外上游 round-trip 掩盖本地可确定的问题。

### 谨慎处理 body 和内存

大请求体、流式数据、兼容转换都要考虑内存成本：

- 不反复复制大 body；
- 不在多个层级重复 decode / encode；
- 不把流式路径退化成全量缓冲，除非该链路明确要求；
- 不为少数场景污染所有请求的 body 处理流程。

### 数据库和调度避免放大成本

后台任务和管理接口也必须遵守性能边界：

- 过滤条件落在正确查询层；
- 避免 N+1；
- 列表接口不默认加载昂贵关联；
- scheduler、outbox、probe、统计任务要考虑索引、批量行为和锁范围；
- cache 必须有清楚作用范围和失效路径。

### 性能优化也要守边界

性能优化不能用全局复杂状态换局部收益，也不能为了某个供应商特例污染所有请求路径。

cache、probe、capability detection、fallback 都必须明确：

- 谁使用；
- 何时更新；
- 何时失效；
- 错误时如何降级；
- 是否会影响无关链路。

一句话：**性能就是不让无关请求为特殊场景买单。**

## Review Checklist

每次 review、同步上游或设计新改动时，至少检查以下问题。

### 边界

- 这个改动属于哪条链路？是否影响了不相关链路？
- 是否把某个模型、供应商、入口的特殊行为放进了通用路径？
- 协议适配、请求构造、retry、billing、scheduler、repository 的职责是否混在一起？
- commit / PR 是否足够小，能单独解释、测试、回滚？

### 优雅

- 这是根因修复，还是错误后的补丁？
- 代码位置是否正确？是否应该放到 adapter、policy、capability 或 repository 层？
- helper 的语义是否清晰？未来能否删除或替换？
- 测试是否证明了边界，而不是只证明某个 happy path？

### 性能

- 是否给热路径增加了不必要解析、复制、转换、IO 或上游请求？
- 是否让无关请求为特殊场景付出成本？
- 是否存在 N+1、重复查询、重复 decode/encode、全量缓冲等放大问题？
- cache、probe、fallback 是否有明确失效和作用范围？

## 结论

本仓库偏好的改动不是“看起来能修”，而是：

- 边界明确；
- 结构自然；
- 作用域可控；
- 根因清楚；
- 热路径克制；
- 测试能证明关键边界；
- 出问题时可以快速定位和回滚。

凡是说不清边界、靠补丁掩盖根因、让无关路径承担成本的改动，都不符合本仓库设计原则。
